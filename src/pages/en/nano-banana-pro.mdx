---
layout: ../../layouts/DocsLayout.astro
title: NanoBanana Pro
lang: en
activeId: nano-banana-pro
---

import CodeExample from '../../components/CodeExample.tsx';

# NanoBanana Pro

NanoBanana Pro is Gemini’s image generation service. It supports multiple aspect ratios (1:1, 16:9, 9:16, etc.) and resolutions (1K, 2K, 4K). All resolutions share the same price; 4K is slower to generate and not recommended. See [Pricing](/pricing?tab=gemini).

**Endpoint**: `POST https://aiberm.com/v1beta/models/gemini-3-pro-image-preview:generateContent`

**Key points**: Send the image description in request body `contents`; set `responseModalities`, `imageConfig` (aspect ratio, resolution) in `generationConfig`. The generated image is in `candidates[0].content.parts` as `inline_data`.

## Example code

Python uses snake_case (e.g. `response_modalities`); REST/cURL uses camelCase (e.g. `responseModalities`). They mean the same.

<CodeExample
  client:load
  examples={[
    {
      key: 'python',
      label: 'Python',
      code: `from google import genai
from google.genai import types

base_url = "https://aiberm.com"
api_key = "YOUR_API_KEY"
prompt = "Generate a cute sea otter image"
model = "gemini-3-pro-image-preview"
output_file = "gemini_sdk_generated.png"

# Image generation config
response_modalities = ["IMAGE"]  # or ["TEXT", "IMAGE"]
aspect_ratio = "16:9"  # "1:1","2:3","3:2","3:4","4:3","4:5","5:4","9:16","16:9","21:9"
image_size = "2K"  # "1K", "2K", "4K" (gemini-3-pro-image-preview only)

# Generation params
temperature = 1.0
top_p = 0.95
max_output_tokens = 8192
enable_google_search = False  # Enable Google Search (adds latency & cost; disable for image-only)

client = genai.Client(
    api_key=api_key,
    http_options=types.HttpOptions(api_version="v1beta", base_url=base_url)
)

config = types.GenerateContentConfig(
    response_modalities=response_modalities,
    temperature=temperature,
    top_p=top_p,
    max_output_tokens=max_output_tokens,
    system_instruction="You are a helpful assistant.",
    image_config=types.ImageConfig(
        aspect_ratio=aspect_ratio,
        image_size=image_size if "pro-image" in model else None
    )
)
if enable_google_search:
    config.tools = [{"google_search": {}}]

response = client.models.generate_content(
    model=model,
    contents=[prompt],
    config=config
)

for part in response.parts:
    if part.text is not None:
        print(part.text)
    elif part.inline_data is not None:
        part.as_image().save(output_file)
        print(f"Image saved: {output_file}")`
    },
    {
      key: 'typescript',
      label: 'TypeScript',
      code: `// ============ Config (centralized) ============
const baseUrl = "https://aiberm.com";
const apiKey = "YOUR_API_KEY";
const prompt = "Generate a cute sea otter image";
const model = "gemini-3-pro-image-preview";
const outputFile = "gemini_sdk_generated.png";
const systemInstruction = "You are a helpful assistant.";

const responseModalities = ["IMAGE"]; // or ["TEXT", "IMAGE"]
const temperature = 1.0; // 0–2, higher = more random
const topP = 0.95; // 0–1, diversity
const maxOutputTokens = 8192; // max tokens per response
const aspectRatio = "16:9"; // "1:1" | "2:3" | "3:2" | "3:4" | "4:3" | "4:5" | "5:4" | "9:16" | "16:9" | "21:9"
const imageSize = "2K"; // "1K" | "2K" | "4K" (gemini-3-pro-image-preview only)
const enableGoogleSearch = false; // Enable Google Search (adds latency & cost; disable for image-only)

async function main() {
  const res = await fetch(\`\${baseUrl}/v1beta/models/\${model}:generateContent\`, {
    method: "POST",
    headers: {
      Authorization: \`Bearer \${apiKey}\`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      contents: [{ parts: [{ text: prompt }] }],
      systemInstruction: { parts: [{ text: systemInstruction }] },
      generationConfig: {
        responseModalities: [...responseModalities],
        temperature,
        topP,
        maxOutputTokens,
        imageConfig: { aspectRatio, imageSize },
      },
      ...(enableGoogleSearch ? { tools: [{ google_search: {} }] } : {}),
    }),
  });
  const data = await res.json();
  for (const part of data.candidates?.[0]?.content?.parts ?? []) {
    if (part.text) console.log(part.text);
    if (part.inlineData) {
      const buf = Buffer.from(part.inlineData.data, "base64");
      require("fs").writeFileSync(outputFile, buf);
      console.log("Image saved:", outputFile);
    }
  }
}
main().catch((e) => {
  console.error(e);
  process.exit(1);
});`
    },
    {
      key: 'curl',
      label: 'cURL',
      code: `curl -X POST "https://aiberm.com/v1beta/models/gemini-3-pro-image-preview:generateContent" \\
  -H "Authorization: Bearer YOUR_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{
    "contents": [{"parts": [{"text": "Generate a cute sea otter image"}]}],
    "systemInstruction": {"parts": [{"text": "You are a helpful assistant."}]},
    "generationConfig": {
      "responseModalities": ["IMAGE"],
      "temperature": 1.0,
      "topP": 0.95,
      "maxOutputTokens": 8192,
      "imageConfig": {"aspectRatio": "16:9", "imageSize": "2K"}
    }
  }'`
    }
  ]}
/>

### cURL image parameters

Below are parameter names (camelCase) for cURL; use snake_case in Python (e.g. `aspectRatio` → `aspect_ratio`).

**imageConfig** (`generationConfig.imageConfig` or SDK `ImageConfig`):

- **aspectRatio** `string` — `1:1`, `2:3`, `3:2`, `3:4`, `4:3`, `4:5`, `5:4`, `9:16`, `16:9`, `21:9`
- **imageSize** `string` — `1K`, `2K`, `4K` (gemini-3-pro-image-preview only)

**Other common generationConfig fields:**

- **responseModalities** `string[]` — `["IMAGE"]` or `["TEXT", "IMAGE"]`
- **temperature** `number` — 0–2, higher = more random
- **topP** `number` — 0–1, diversity
- **maxOutputTokens** `number` — e.g. 8192
- **systemInstruction** `object` — e.g. `{"parts": [{"text": "..."}]}`
- **tools** `array` — e.g. `[{"google_search": {}}]`

## Enabling Google Search

Only the Gemini-style API supports Google Search. Pass `tools` in the config; the model will use real-time web info when needed.

**Not recommended by default:** Adds latency and cost; unnecessary for image-only use. Turn off if you don’t need search.

**Python (SDK):**

```python
config = types.GenerateContentConfig(
    # ... other params like response_modalities, image_config
    tools=[{"google_search": {}}]
)
response = client.models.generate_content(model=model, contents=[prompt], config=config)
```

**cURL (REST):**

Add `tools` at the top level of the request body:

```bash
curl -X POST "https://aiberm.com/v1beta/models/gemini-3-pro-image-preview:generateContent" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{"parts": [{"text": "Your question"}]}],
    "generationConfig": { ... },
    "tools": [{"google_search": {}}]
  }'
```

## FAQ

- **No image in response:** Ensure model is `gemini-3-pro-image-preview` and the prompt is a clear image description.
- **Decode error:** Use only the base64 string after `data:image/...;base64,` and remove newlines before decoding.
