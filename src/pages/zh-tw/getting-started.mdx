---
layout: ../../layouts/DocsLayout.astro
title: 快速開始
lang: zh-tw
activeId: getting-started
---

import CodeExample from '../../components/CodeExample.tsx';
import Callout from '../../components/Callout.tsx';

# 快速開始

Aiberm 提供統一的 API 介面，透過單一端點即可存取數百個 AI 模型。只需簡單的整合流程，幾分鐘就能開始使用。

<Callout client:load type="tip" title="提示">
正在尋找免費模型與速率限制的資訊？請查看常見問題。
</Callout>

## 使用 OpenAI SDK

最簡單的方式是使用官方的 OpenAI SDK，只需更改 base_url 即可開始使用。

<CodeExample
  client:load
  examples={[
    {
      key: 'python',
      label: 'Python',
      code: `from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key-here",
    base_url="https://aiberm.com/v1"
)

response = client.chat.completions.create(
    model="google/gemini-2.5-flash",
    messages=[
        {"role": "user", "content": "Hello!"}
    ]
)

print(response.choices[0].message.content)`
    },
    {
      key: 'typescript',
      label: 'TypeScript',
      code: `import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: 'sk-your-api-key-here',
  baseURL: 'https://aiberm.com/v1'
});

const response = await openai.chat.completions.create({
  model: 'google/gemini-2.5-flash',
  messages: [
    { role: 'user', content: 'Hello!' }
  ]
});

console.log(response.choices[0].message.content);`
    }
  ]}
/>

## 直接使用 Aiberm API

您可以使用 HTTP 請求直接呼叫 API。

<Callout client:load type="info" title="資訊">
使用我們的互動式請求建構器來產生 API 請求。
</Callout>

<CodeExample
  client:load
  examples={[
    {
      key: 'python',
      label: 'Python',
      code: `import requests

response = requests.post(
    "https://aiberm.com/v1/chat/completions",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "google/gemini-2.5-flash",
        "messages": [
            {"role": "user", "content": "Hello!"}
        ]
    }
)

print(response.json())`
    },
    {
      key: 'typescript',
      label: 'TypeScript',
      code: `const response = await fetch('https://aiberm.com/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'google/gemini-2.5-flash',
    messages: [{ role: 'user', content: 'Hello!' }]
  })
});

const data = await response.json();
console.log(data);`
    },
    {
      key: 'curl',
      label: 'Curl',
      code: `curl https://aiberm.com/v1/chat/completions \\
  -H "Content-Type: application/json" \\
  -H "Authorization: Bearer YOUR_API_KEY" \\
  -d '{
    "model": "google/gemini-2.5-flash",
    "messages": [
      {"role": "user", "content": "Hello!"}
    ]
  }'`
    }
  ]}
/>

## 下一步

我們的 API 還支援串流傳輸、批次處理等進階功能。若要了解更多資訊，請查看聊天完成 API 的詳細文件。

API 也支援串流傳輸。

## 支援的模型

我們支援多種 AI 模型，包括但不限於：

### OpenAI 模型

- GPT-4
- GPT-4 Turbo
- GPT-3.5 Turbo

### Anthropic 模型

- Claude-3 Opus
- Claude-3 Sonnet
- Claude-3 Haiku

### 其他模型

- Gemini Pro
- Llama 2
- 更多...
